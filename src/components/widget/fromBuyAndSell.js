import React, { useState, useEffect} from "react";
import { Modal, Form, Input, Button } from "antd";

const  FromBuyAndSell = (props) => {

  const [visible, setVisible] = useState(
    props.visible)
  useEffect(() => {
  }, [])

    return (
      <>

      </>
    );
}

export default FromBuyAndSell
